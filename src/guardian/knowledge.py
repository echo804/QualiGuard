"""QualiGuard Knowledge Base

Chinese explanations and fix suggestions for all rules.
"""

from __future__ import annotations


RULE_ADVICE: dict[str, dict[str, str]] = {
  "STA000": {
    "title": "Python 语法错误",
    "why": "文件包含语法错误，Python 解析器无法理解代码结构。常见原因：括号不匹配、引号未闭合、缩进错误、遗漏逗号等。",
    "fix": "查看提示的行号定位具体位置，修正后文件才能正常运行。建议使用 IDE 或 ruff 自动检测语法问题。"
  },
  "STA001": {
    "title": "函数过长",
    "why": "函数语句超过 50 行，说明该函数承担了太多职责。过长的函数难以阅读、测试和维护。",
    "fix": "将函数拆分为多个单一职责的小函数，每个函数做一件事。符合单一职责原则（Single Responsibility Principle）。"
  },
  "SEC001": {
    "title": "硬编码密码",
    "why": "代码中直接写入了明文密码。如果提交到 Git，密码会永久暴露在版本历史中，属于严重安全漏洞。",
    "fix": "将密码移入 .env 文件或密钥管理服务，运行时通过 os.getenv() 读取。添加 .env 到 .gitignore。"
  },
  "SEC002": {
    "title": "硬编码 API Key",
    "why": "代码中直接写入了 API 密钥。一旦泄露，攻击者可用它调用你的 API 接口，导致资源滥用、账单飙升。",
    "fix": "将 API Key 存入环境变量或密钥管理服务，通过 os.getenv() 读取。使用前检查环境变量是否存在。"
  },
  "SEC003": {
    "title": "硬编码 Secret",
    "why": "代码中直接写入了敏感密钥（如翻译密钥、签名密钥等）。与密码和 API Key 同样等级的安全风险。",
    "fix": "移至环境变量或密钥管理服务。分开开发/测试/生产环境的密钥配置。"
  },
  "SEC004": {
    "title": "危险的 eval() 调用",
    "why": "eval() 会执行任意 Python 代码。如果传入用户输入，攻击者可以通过特殊构造的输入执行任意命令，导致代码注入攻击。",
    "fix": "用更安全的替代方案：int()/float() 转数字、ast.literal_eval() 解析字典列表、或自定义解析器限制可执行操作。"
  },
  "CPX001": {
    "title": "圈复杂度过高",
    "why": "函数的圈复杂度超过 10，意味着该函数有太多分支路径（if/for/while/except/逻辑运算等）。过高的复杂度导致代码难以测试和维护。",
    "fix": "将复杂的逻辑拆分为多个小函数，每个函数保持单一分支路径。可以使用策略模式（Strategy Pattern）等设计模式优化。"
  },
  "DEP001": {
    "title": "已知漏洞的依赖包",
    "why": "项目使用的第三方包版本存在已公开的安全漏洞（CVE）。使用有漏洞的依赖可能被攻击者利用。",
    "fix": "升级到最新安全版本。建议定期运行 pip audit 或使用 Dependabot 自动检测依赖安全。"
  },
  "STYF401": {
    "title": "导入但未使用的模块",
    "why": "导入了某个模块但后续从未使用。多余导入增加启动时间和代码帖的阅读负担。",
    "fix": "删除未使用的 import 语句。如果是为了旁边效应（如注册插件），用 # noqa: F401 注释忽略。"
  },
  "STYF841": {
    "title": "未使用的变量",
    "why": "变量被赋值后从未被使用，属于无用代码。可能是重构遗留或逻辑缺失，误导后续维护者。",
    "fix": "如果确实不需要，直接删除该变量；如果是逻辑遗漏，补充使用该变量的代码。"
  },
  "STYE741": {
    "title": "模糊的变量名",
    "why": "使用了容易与数字混淆的变量名（如字母 `l` 与数字 `1`，字母 `O` 与数字 `0`）。这会导致代码阅读困难和潜在的 bug。",
    "fix": "将这些变量重命名为有意义的名称，避免使用单字母或容易混淆的字符。例如将 `l` 改为 `length`。"
  }
}